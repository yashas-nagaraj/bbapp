const express=require('express'),bodyParser=require('body-parser'),cors=require('cors'),{Pool}=require('pg');const app=express();app.use(cors());app.use(bodyParser.json());const pool=new Pool({host:process.env.DB_HOST||'localhost',port:5432,user:process.env.DB_USER||'postgres',password:process.env.DB_PASSWORD||'pass',database:process.env.DB_NAME||'bbapp'});const seasons=[{season:1,episodes:7,summary:'Intro'},{season:2,episodes:13,summary:'More'}];app.get('/api/seasons',(req,res)=>res.json(seasons));app.listen(3000,()=>console.log('backend running'));